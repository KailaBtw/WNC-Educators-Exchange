---
import SectionHeader from "../ui/SectionHeader.astro";
import EventCard from "../events/EventCard.astro";
import Button from "../ui/Button.astro";
import { events } from "../../content/events";
---

<section class="section" id="events">
  <div class="container">
    <div data-reveal>
      <SectionHeader eyebrow="Events" title="Event information">
        <p>
          Educator perspectives through summits, helped shape the Top 10 Issues report, detailing the top issues in AI that are facing educators. The next summit is November 17 —
          register from the upcoming event card.
        </p>
      </SectionHeader>
    </div>
    <div class="events-grid">
      {
        events.map((event, index) => (
          <div data-reveal data-reveal-delay={String(0.06 * index)} data-reveal-y="40">
            <EventCard event={event} />
          </div>
        ))
      }
    </div>
    <div class="events-grid__cta" data-reveal data-reveal-delay="0.14" data-reveal-y="28">
      <Button href="/events">Explore All Events</Button>
    </div>
  </div>
</section>

<style>
  .events-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1.5rem;
  }

  .events-grid__cta {
    display: flex;
    justify-content: center;
    margin-top: 2.25rem;
  }

  @media (max-width: 900px) {
    .events-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
